# 涟漪观察 · 技术架构与部署说明

## 一、数据存储机制（当前）

当前未使用关系型数据库，而是采用 **JSON 文件持久化**。所有数据都保存在项目目录的 `data/` 文件夹下：

| 文件 | 内容 | 读写方式 |
|------|------|----------|
| `data/users.json` | 注册用户（ID、账号、scrypt 密码哈希、会员状态、每日免费额度） | `src/store.js` 全量读/写 |
| `data/articles.json` | 文章（标题、栏目、导语、免费/专享 HTML、影响标签、来源、发布时间） | 全量读/写 |
| `data/orders.json` | 订单（套餐、金额、支付状态、小鹅通/微信订单号） | 全量读/写 |
| `data/settings.json` | 运营配置（AI 模型提供商、RSS 源、提示词、调度规则） | `src/settings.js` 全量读/写 |
| `data/automation.log` | 自动化调度日志（抓取/生成/发布条数） | `src/scheduler.js` append 追加 |

### 机制特点

- **读**：启动/请求时 `JSON.parse(fs.readFileSync(...))` 把整个文件读到内存。
- **写**：修改后 `fs.writeFileSync(..., JSON.stringify(data, null, 2))` 全量覆写。
- **无事务、无锁、无并发控制**：多进程同时写同一文件可能出现竞争/覆盖，因此必须**单进程运行**。
- **备份简单**：直接把 `data/` 目录压缩或复制一份即可。

### 适用边界

| 场景 | 表现 | 建议 |
|------|------|------|
| 个人/小团队、日均几十篇内容、几百用户 | 完全够用，部署运维极简 | 当前方案即可 |
| 大量用户同时注册/付费/写评论 | 文件竞争风险，可能丢数据 | 必须切 SQLite/PostgreSQL |
| 内容量 > 几千篇 | 每次全量读写内存占用大 | 改用数据库 + 分页读取 |
| 需要多人/多机部署 | 文件状态不一致 | 必须上数据库 |

### 迁移方案（不改业务代码）

`src/store.js` 的接口（`getUsers / saveUsers / findUserByAccount / addArticle` 等）已经全部抽象好。要迁移时，只需重写 `src/store.js` 内部实现，把文件读写换成 SQLite/PostgreSQL 调用，**其他业务代码无需改动**。

建议顺序：
1. **当前阶段**：JSON 文件（验证 MVP）。
2. **用户量过千**：换成 `better-sqlite3`，单文件数据库，零运维。
3. **用户量过万/商业化放量**：PostgreSQL + 连接池（`pg`）。

---

## 二、部署平台与资源评估

### 当前已部署的形态

| 形态 | 地址/入口 | 平台 | 说明 |
|------|-----------|------|------|
| 公开静态预览版 | `https://5d3391d1f6bb4586aa91019f66f26ce3.app.workbuddy.link` | WorkBuddy CloudStudio 沙箱 | 纯静态 HTML，会员按钮跳小鹅通，无后端。用于免备案快速展示。 |
| 完整 Node 版（本地） | `http://localhost:3100`（或 `3000`） | 本机 Node 22 | 含注册/登录/会员/付费墙/后台/自动化调度。 |

> 当前本地端口 `3000` 被上一轮旧进程占用且无法强制结束，所以新代码跑在 `3100`。你访问 `http://localhost:3100` 即可。

### 各平台对比

| 平台 | 免费额度 | 并发能力 | 适合用户数 | 备案要求 | 备注 |
|------|----------|----------|------------|----------|------|
| **WorkBuddy CloudStudio** | 免费沙箱，带宽/时长受平台限制 | 静态页，无并发概念 | 演示用 | 境外节点，免备案 | 仅适合验收/预览，**不要当正式生产环境** |
| **Railway** | 有免费/试用额度；付费约 $5/月起 | 1 vCPU/512MB 起步，Node 单进程能扛 100-500 并发读 | 千人级 | 境外服务器，免备案 | 支持持久化磁盘，推荐 MVP 海外部署 |
| **Render** | 免费 Web Service：15 分钟无访问自动休眠，512MB 内存，100GB/月流量 | 休眠后有冷启动（约 5-10 秒） | 测试/小流量 | 境外，免备案 | 免费档适合做展示，付费 $7+/月 可去掉休眠 |
| **Vercel** | 免费 Hobby：100GB 流量/月，Serverless Functions | Serverless 并发高，但**无持久化磁盘** | 需外接数据库 | 境外，免备案 | 本项目用 JSON 文件的话不合适；接上 PostgreSQL 才好用 |
| **腾讯云 CloudBase / 云函数** | 有免费额度（调用次数/出流量） | 按量弹性 | 需外接数据库 | 境内需 ICP 备案 | 适合小程序/H5，本项目的文件存储需额外改造 |
| **腾讯云轻量应用服务器 / 阿里云 ECS（香港节点）** | 无免费，约 ¥30-80/月起 | 1核1G 可轻松跑本项目 | 千人级（JSON）/ 万人级（SQLite） | 香港节点免备案 | 性价比最高的正式海外方案 |
| **腾讯云轻量 / 阿里云 ECS（中国大陆节点）** | 无免费，约 ¥40-100/月起 | 同上 | 同上 | **必须 ICP 备案** | 长期合规首选，但需要营业执照相关资质 |

### 并发与用户数经验值

这些数字是**经验估算**，不是精确压测结果：

- **读请求**：首页/文章列表是 JSON 文件读 + 模板渲染，Node 单进程可轻松处理 **300-1000 QPS**（受 CPU/内存限制，主要是 EJS 渲染）。
- **写请求**：注册/登录/发布/订阅 都会触发文件全量写入，单进程串行处理。纯 JSON 文件下，**建议控制在 10 并发写以内**；超过后响应明显变慢且有损坏风险。
- **用户数**：
  - JSON 文件方案：安全上限约 **1000 活跃用户**（日活）。
  - SQLite 方案：可支撑 **1-10 万注册用户**（日活几千）。
  - PostgreSQL 方案：可支撑 **10 万级以上**。

### 推荐部署路径

按你现在的状态（无营业执照、想先跑起来验证）：

1. **现在**：继续用 WorkBuddy CloudStudio 公开预览版做展示；本地 `3100` 端口做管理/内容生产。
2. **一周后**：如果流量和内容稳定了，买一个 **香港轻量服务器**（腾讯云/阿里云，约 ¥30-50/月），把完整 Node 版放上去，数据文件用 `data/` 目录，并加 `systemd`/`pm2` 守护进程。
3. **一个月后**：用户/内容量上来，把 `src/store.js` 迁移到 SQLite（`better-sqlite3`），保留同样接口，服务器配置升级到 1核2G。
4. **长期**：如果月流水超过几万，注册公司、办 ICP 经营许可证（EDI）、迁回境内机房、接微信支付官方商户号。

---

## 三、上线前必须改的配置

### 1. 支付/会员

`config.json`：

```json
"payment": {
  "mode": "external",
  "external": {
    "provider": "xiaoe",
    "shopUrl": "https://8mhe0.xet.tech/s/3VK78h",
    "monthUrl": "https://8mhe0.xet.tech/s/3VK78h",
    "yearUrl": "https://8mhe0.xet.tech/s/27WUSn",
    "returnPath": "/member/return",
    "demoActivate": false
  }
}
```

- `demoActivate: false` 后，用户必须从小鹅通真实回跳才能激活会员；生产强烈建议接小鹅通 API 做订单真实性校验。

### 2. 管理员账号

默认超级管理员 `admin / Admin@888` 仅用于验收。上线前：

- 登录后台 → 不要再使用弱密码；或直接在 `data/users.json` 中删除该预置账号，改用自己注册的账号并手动修改 `role: 'admin'`。

### 3. AI 模型与 RSS

后台「自动化设置」里：

- 把 `mock` 模型切换为你真实提供商（如 DashScope：填 `https://dashscope.aliyuncs.com/compatible-mode/v1` + `sk-...` + `qwen-plus`）。
- 把 RSS 模式从 `mock` 切到 `live`。
- 调提示词，让它更符合你的栏目风格。

### 4. 合规

- 境外节点：免 ICP 备案，但国内访问速度和稳定性一般。
- 境内节点：必须 ICP 备案；经营性会员收费严格来说需 EDI 证，个体户办不了，所以走小鹅通/有赞（平台持证）是最稳妥的过渡方案。
- 必须页面：《隐私政策》《用户协议》《广告标识》。

---

## 四、本地运行与部署命令

```bash
cd D:/cache集合/wb/2026-08-18-21-07-17/ripple-app
npm install
npm start          # 默认 http://localhost:3000
npm test           # 26 项核心自测
node test/automation_selftest.js  # 18 项自动化自测
```

> 当前因为 3000 端口被旧进程占用，建议用 `PORT=3100 npm start`。

---

## 五、一句话总结

- **数据库**：现在是 JSON 文件，够用但非生产级；到千人级换 SQLite，到万人级换 PostgreSQL。
- **部署**：短期用 WorkBuddy CloudStudio 预览 + 香港轻量服务器跑完整版；长期合规迁回境内并换公司主体。
- **并发**：读没问题，写（注册/付费/发布）受文件存储限制，单进程百人级安全。
